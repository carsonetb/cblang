class Main (int alpha, string beta) = (
    const int x = 0,
    scope func -> string = {
        return 2;
    },
    class Sub<T, U> (int alpha2, char beta2) = (

    )//,
    // Sub v = Sub(1, 'd')
) 